# APIRest Marion PLAYOUT EPSI B3 groupe 1 

une fois que le projet a été récupéré lancer dans le repertoire principal la commande : 

docker-compose up --detach --build


lancer http://localhost:8080
