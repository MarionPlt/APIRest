import { SearchClientRequest } from './search-client-request';

describe('SearchClientRequest', () => {
  it('should create an instance', () => {
    expect(new SearchClientRequest()).toBeTruthy();
  });
});
